# ✅ Чеклист развертывания системы смены статусов

## 📋 Перед развертыванием

### 1. Проверка кода
- [x] Все файлы созданы
- [x] Нет ошибок компиляции TypeScript
- [x] Парсер алиасов реализован
- [x] Telegram бот обработчик добавлен
- [x] API endpoints созданы
- [x] Маршруты подключены

### 2. База данных
- [ ] Миграция 008 применена
- [ ] Таблица `album_status_history` создана
- [ ] Статусы обновлены в `album_statuses`
- [ ] Проверить ENUM в таблице `album_statuses`

### 3. Конфигурация
- [ ] `TELEGRAM_BOT_TOKEN` установлен в `.env`
- [ ] База данных настроена
- [ ] Подключение к БД работает

---

## 🚀 Шаги развертывания

### Шаг 1: Остановить сервер
```bash
# Остановить текущий процесс сервера
# Ctrl+C или kill процесс
```

### Шаг 2: Применить миграции
```bash
cd server

# Вариант 1: Через npm скрипт
npm run migrate

# Вариант 2: Вручную через MySQL
mysql -u root -p klamonline < migrations/008_add_album_status_history.sql
```

### Шаг 3: Проверить изменения в БД
```sql
USE klamonline;

-- Проверить новые статусы
SELECT * FROM album_statuses;

-- Проверить таблицу истории
DESCRIBE album_status_history;

-- Проверить ENUM статусов
SHOW COLUMNS FROM album_statuses WHERE Field = 'code';
```

Ожидаемый результат:
```
+-------+---------------------------------------------------------------------+
| Field | Type                                                                |
+-------+---------------------------------------------------------------------+
| code  | enum('waiting','upload','sent','accepted','remarks','production')  |
+-------+---------------------------------------------------------------------+
```

### Шаг 4: Пересобрать проект
```bash
cd server

# Установить зависимости (если нужно)
npm install

# Пересобрать TypeScript
npm run build
```

### Шаг 5: Запустить сервер
```bash
cd server
npm run dev
```

### Шаг 6: Проверить логи
Должны появиться строки:
```
✅ Database connected
🤖 Telegram bot started
🚀 Server is running on port 3001
```

---

## 🧪 Тестирование

### Тест 1: Парсер алиасов
```bash
cd server
npx ts-node src/utils/testStatusAliases.ts
```

Ожидаемый результат: Все тесты пройдены ✅

### Тест 2: Telegram бот (базовая проверка)
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start` в личные сообщения
4. Бот должен ответить приветственным сообщением

### Тест 3: Подключение к каналу
1. Создайте тестовый канал в Telegram
2. Добавьте бота в канал (сделайте администратором)
3. Бот должен отправить сообщение с ID канала
4. Скопируйте ID канала

### Тест 4: Создание проекта
1. Войдите в KLAM.Online
2. Создайте новый проект
3. При создании вставьте ID Telegram канала
4. Создайте тестовый альбом (например, АР-001)

### Тест 5: Смена статуса через Telegram
1. Напишите в канале: `АР-001 ок`
2. Бот должен ответить: `✅ Альбом АР-001 → Принято`
3. Проверьте в БД:
```sql
SELECT a.code, ast.name as status, a.last_status_at
FROM albums a
JOIN album_statuses ast ON a.status_id = ast.id
WHERE a.code = 'АР-001';
```

### Тест 6: История изменений
Проверьте запись в истории:
```sql
SELECT 
  h.created_at,
  a.code,
  old_st.name as old_status,
  new_st.name as new_status,
  h.changed_by_telegram_id
FROM album_status_history h
JOIN albums a ON h.album_id = a.id
LEFT JOIN album_statuses old_st ON h.old_status_id = old_st.id
JOIN album_statuses new_st ON h.new_status_id = new_st.id
WHERE a.code = 'АР-001'
ORDER BY h.created_at DESC;
```

### Тест 7: API endpoints
```bash
# Получить токен авторизации (замените на реальный)
TOKEN="your_jwt_token_here"

# Тест смены статуса
curl -X PUT http://localhost:3001/api/companies/1/projects/1/albums/1/status \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"statusCode": "remarks", "comment": "Нужно исправить размеры"}'

# Тест получения истории
curl http://localhost:3001/api/companies/1/projects/1/albums/1/status-history \
  -H "Authorization: Bearer $TOKEN"
```

---

## ✅ Критерии успеха

Система работает правильно, если:

- [x] ✅ Сервер запускается без ошибок
- [x] ✅ Telegram бот активен (логи показывают "Telegram bot started")
- [ ] ✅ Бот реагирует на команды в канале
- [ ] ✅ Статусы альбомов меняются в БД
- [ ] ✅ История изменений записывается
- [ ] ✅ Бот отправляет подтверждения
- [ ] ✅ API endpoints работают
- [ ] ✅ История доступна через API

---

## 🐛 Решение проблем

### Проблема: Бот не запускается
**Симптомы:** В логах: `❌ TELEGRAM_BOT_TOKEN not set in .env`

**Решение:**
```bash
# Проверьте .env файл
cat server/.env | grep TELEGRAM_BOT_TOKEN

# Добавьте токен, если его нет
echo "TELEGRAM_BOT_TOKEN=your_token_here" >> server/.env
```

### Проблема: Ошибка миграции
**Симптомы:** `ERROR 1064: You have an error in your SQL syntax`

**Решение:**
1. Проверьте версию MySQL (должна быть 8.0+)
2. Проверьте кодировку базы данных
3. Примените миграцию вручную через MySQL Workbench

### Проблема: Бот не реагирует на команды
**Симптомы:** Пишете команды, но бот молчит

**Решение:**
1. Проверьте логи сервера на наличие ошибок
2. Убедитесь, что бот добавлен в канал как администратор
3. Проверьте, что канал привязан к проекту:
```sql
SELECT * FROM project_channels WHERE telegram_chat_id = 'YOUR_CHAT_ID';
```

### Проблема: Альбом не найден
**Симптомы:** Бот отвечает "Альбом не найден"

**Решение:**
1. Проверьте код альбома (должен быть формата АР-001)
2. Убедитесь, что альбом создан в правильном проекте
3. Проверьте в БД:
```sql
SELECT a.code, p.name as project
FROM albums a
JOIN projects p ON a.project_id = p.id
WHERE a.code = 'АР-001';
```

### Проблема: Статус не меняется
**Симптомы:** Бот подтверждает, но статус остается прежним

**Решение:**
1. Проверьте транзакции в БД (возможно, откат)
2. Проверьте права пользователя на запись в БД
3. Посмотрите логи на наличие ошибок SQL

---

## 📊 Мониторинг

### Логи для отслеживания
```bash
# Следить за логами сервера
cd server
npm run dev | grep -E "(Album|Status|Telegram)"

# Отфильтровать только ошибки
npm run dev 2>&1 | grep "❌"
```

### Полезные SQL запросы

**Статистика по сменам статусов:**
```sql
SELECT 
  DATE(created_at) as date,
  COUNT(*) as status_changes
FROM album_status_history
GROUP BY DATE(created_at)
ORDER BY date DESC;
```

**Самые активные пользователи:**
```sql
SELECT 
  changed_by_telegram_id,
  COUNT(*) as changes_count
FROM album_status_history
WHERE changed_by_telegram_id IS NOT NULL
GROUP BY changed_by_telegram_id
ORDER BY changes_count DESC
LIMIT 10;
```

**Альбомы с наибольшим количеством изменений:**
```sql
SELECT 
  a.code,
  a.name,
  COUNT(h.id) as status_changes
FROM albums a
LEFT JOIN album_status_history h ON a.id = h.album_id
GROUP BY a.id
ORDER BY status_changes DESC
LIMIT 10;
```

---

## 📚 Документация

После развертывания убедитесь, что доступны:

- [ ] `STATUS_ALIASES_GUIDE.md` - полное руководство
- [ ] `USER_GUIDE_TELEGRAM.md` - инструкция для пользователей
- [ ] `TESTING_STATUS_CHANGES.md` - гайд по тестированию
- [ ] `ARCHITECTURE_DIAGRAM.md` - архитектура системы
- [ ] `IMPLEMENTATION_SUMMARY.md` - резюме реализации

---

## 🎉 Готово!

Если все чекбоксы отмечены, система полностью развернута и работает!

**Следующие шаги:**
1. Обучить пользователей (раздать `USER_GUIDE_TELEGRAM.md`)
2. Мониторить первые дни использования
3. Собирать обратную связь
4. Планировать улучшения

---

**Дата развертывания:** _________________

**Развернул:** _________________

**Версия:** 1.0.0
