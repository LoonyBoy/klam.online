# KLAM.Online - Быстрый старт

## 🚀 Начало работы

### 1. Предварительные требования

Убедитесь, что у вас установлены:
- ✅ Node.js 18+ ([скачать](https://nodejs.org/))
- ✅ MySQL 8+ ([скачать](https://dev.mysql.com/downloads/mysql/))
- ✅ npm или yarn

### 2. Установка зависимостей

```bash
# Перейти в папку сервера
cd server

# Установить зависимости
npm install
```

### 3. Настройка базы данных

#### Шаг 3.1: Создайте файл .env

```bash
# Скопируйте шаблон
cp .env.example .env
```

#### Шаг 3.2: Отредактируйте .env

Откройте `server/.env` и укажите свои данные:

```env
# Подключение к MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ВАШ_ПАРОЛЬ_MYSQL
DB_NAME=klamonline
```

#### Шаг 3.3: Запустите миграции

```bash
# Создать базу данных и применить схему
npm run db:reset
npm run db:migrate
```

**Важно:** `db:reset` удалит существующую базу данных! Используйте только при первой установке.

### 4. Запуск сервера

```bash
# Режим разработки (с hot-reload)
npm run dev
```

Сервер запустится на `http://localhost:3001`

### 5. Проверка работы

Откройте в браузере:
- **Health Check:** http://localhost:3001/health
- **API Info:** http://localhost:3001/api

Если видите ответ JSON - всё работает! 🎉

---

## 📂 Структура проекта

```
server/
├── config/              # Конфигурация
│   └── database.ts      # Настройки БД
├── migrations/          # SQL миграции
│   ├── 001_initial_schema.sql
│   └── 002_seed_dictionaries.sql
├── scripts/             # Утилиты
│   ├── migrate.ts       # Применить миграции
│   └── reset.ts         # Сбросить БД
├── src/
│   ├── controllers/     # Контроллеры API
│   ├── middleware/      # Middleware
│   ├── models/          # Модели данных
│   ├── routes/          # Маршруты
│   ├── db.ts            # Connection pool
│   └── index.ts         # Точка входа
├── .env                 # Переменные окружения (не коммитить!)
├── .env.example         # Шаблон .env
├── package.json
└── tsconfig.json
```

---

## 🔧 Полезные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в режиме разработки |
| `npm run build` | Компиляция TypeScript |
| `npm start` | Запуск prod версии |
| `npm run db:migrate` | Применить миграции |
| `npm run db:reset` | ⚠️ Сбросить БД (удалит данные!) |

---

## 🗄️ Структура базы данных

### Основные сущности

1. **Users** - пользователи системы (логин через Telegram)
2. **Companies** - компании (multi-tenant)
3. **Projects** - проекты компании
4. **Albums** - альбомы проекта с отслеживанием статусов
5. **Participants** - исполнители и заказчики
6. **Departments** - отделы (КР, АР, ЭС, СС, ГП, ОВКВ)

### Связи

```
companies → projects → albums
    ↓           ↓
 users    participants
```

---

## ❓ Устранение проблем

### Ошибка подключения к MySQL

**Проблема:** `Error: Access denied for user...`

**Решение:**
1. Проверьте правильность пароля в `.env`
2. Убедитесь, что MySQL запущен
3. Создайте пользователя MySQL (если нужно):

```sql
CREATE USER 'klamuser'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON klamonline.* TO 'klamuser'@'localhost';
FLUSH PRIVILEGES;
```

### База данных не создаётся

**Проблема:** `Unknown database 'klamonline'`

**Решение:**
```bash
npm run db:reset
npm run db:migrate
```

### Порт занят

**Проблема:** `Port 3001 is already in use`

**Решение:** Измените порт в `.env`:
```env
PORT=3002
```

---

## 📖 Дальнейшие шаги

1. ✅ Настроить аутентификацию через Telegram
2. ✅ Создать API endpoints для компаний
3. ✅ Реализовать CRUD для проектов и альбомов
4. ✅ Интегрировать Telegram Bot
5. ✅ Добавить WebSocket для real-time обновлений

---

## 📝 Лицензия

Proprietary - KLAM.Online

---

**Нужна помощь?** Проверьте `server/README.md` для подробной документации.
